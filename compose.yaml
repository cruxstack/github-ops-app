services:
  server:
    build: .
    container_name: github-ops-server
    env_file: .env
    ports:
      - "${APP_PORT:-8080}:${APP_PORT:-8080}"
    volumes:
      - ./.local:/.local:ro
    restart: unless-stopped
